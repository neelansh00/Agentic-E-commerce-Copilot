# Python 3.12 is the verified minor version. Phases 1–2 remain standard-library-only.
# Direct Phase 3 dependencies pinned to the versions tested in the project venv.
openai==3.16.0
pydantic==2.13.5
sqlglot==30.18.0
python-dotenv==1.2.3

# Phase 4: local CPU embeddings and exact vector search.
fastembed==0.8.0
faiss-cpu==1.15.1
numpy==2.5.3
huggingface-hub==1.32.0
tokenizers==0.23.2

# Phase 6: chat interface and its dataframe/chart runtime.
streamlit==1.64.0
pandas==3.0.6
altair==6.3.0
