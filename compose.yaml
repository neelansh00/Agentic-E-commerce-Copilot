services:
  copilot:
    build: .
    ports:
      - "127.0.0.1:8501:8501"
    volumes:
      - type: bind
        source: ./data
        target: /app/data
        read_only: true
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-}
